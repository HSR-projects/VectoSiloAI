{
  "name": "incogni-ai-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "IncogniAI Android wrapper (Capacitor) — native shell around the hosted web app.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug",
    "build:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
